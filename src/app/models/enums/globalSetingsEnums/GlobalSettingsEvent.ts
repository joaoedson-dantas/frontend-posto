export enum SettingsEvent {
  ADD_SETTING_EVENT = 'Adicionar configuração',
  EDIT_SETTING_EVENT = 'Editar configuração',
}

/* Tipar cada um dos enventos que podem ocorrer e enviar para o componete pai através do output. */
